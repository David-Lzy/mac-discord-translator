# Upstream Dependencies / References

This export does not include third-party upstream source trees.

## Reaction translation bot reference

The local workflow documented in this stack can integrate with the upstream DiscordTranslationBot project.
In the original local workspace it lived at:

- `scripts/DiscordTranslationBot-upstream`

That upstream source was intentionally excluded from this export to avoid redistributing another project's code tree here.

## What is still included

- setup documentation for integrating a reaction-based bot with the local LibreTranslate-compatible API
- the local MLX translation API wrapper
- the custom mirror bot used for multilingual mirrored chat channels
